class Category {
    // initialize object on class w/ constructor
    constructor(id, title, color) {
        this.id = id
        this.title = title
        this.color = color
    }
}

export default Category